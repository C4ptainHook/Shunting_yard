#pragma once
#include "Parser.h"

namespace Algorithm {
    queue<Parser::Token> Shunting_yard(queue<Parser::Token>& parsed_q) {
        queue<Parser::Token>::iterator it;
        for (it = parsed_q.begin(); ) {


        }
    }

}
